
/**
 * 
 */

 import {polygon} from "@/leetcode/other";
describe('other测试',()=>{
  it.only('多边形',()=>{
    let arr = [
      [1,1],
      [1,2],
      [2,2],
      [2,3],
      [4,3],
      [4,1]
    ];
    // let rs = polygon(arr,4);
    // console.log(rs);
  })
}) 